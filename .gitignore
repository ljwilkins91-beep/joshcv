# dependencies
/node_modules

# next.js build output
/.next/
/out/
/build

# typescript / next generated
next-env.d.ts
*.tsbuildinfo

# env files (secrets live in Vercel, never in the repo)
.env
.env*.local

# local tooling / preview config (may contain throwaway creds)
.claude/

# misc
.DS_Store
npm-debug.log*
